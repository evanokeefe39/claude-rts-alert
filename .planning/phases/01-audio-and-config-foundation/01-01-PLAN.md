---
phase: 01-audio-and-config-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/audio/player.ts
  - src/audio/player.test.ts
  - src/audio/index.ts
  - assets/sounds/placeholder.wav
autonomous: true
requirements: [PLAY-01, PLAY-02]

must_haves:
  truths:
    - "A test script can play a .wav file on Windows, macOS, and Linux without native compilation"
    - "Sound files are loadable from the package's bundled assets directory"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with play-sound dependency"
      contains: "play-sound"
    - path: "src/audio/player.ts"
      provides: "Cross-platform audio playback module"
      exports: ["playSound", "resolveAssetPath"]
    - path: "src/audio/player.test.ts"
      provides: "Tests for audio playback and asset resolution"
      min_lines: 30
    - path: "assets/sounds/placeholder.wav"
      provides: "Bundled test sound file"
  key_links:
    - from: "src/audio/player.ts"
      to: "assets/sounds/"
      via: "path.resolve from package root"
      pattern: "path\\.(resolve|join).*assets.*sounds"
    - from: "src/audio/player.ts"
      to: "play-sound"
      via: "require/import"
      pattern: "play-sound"
---

<objective>
Initialize the claude-notify project and build the cross-platform audio playback module.

Purpose: Establishes the project foundation (package.json, TypeScript config, directory structure) and delivers the core audio playback capability that all downstream features depend on. The play-sound npm package provides cross-platform audio without native compilation by delegating to OS audio tools (PowerShell on Windows, afplay on macOS, aplay on Linux).

Output: A working Node/TypeScript project with a tested audio player module that can resolve bundled sound assets and play them on any platform.
</objective>

<execution_context>
@C:/Users/evano/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/evano/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project scaffold and bundled assets</name>
  <files>package.json, tsconfig.json, assets/sounds/placeholder.wav, src/audio/index.ts</files>
  <action>
1. Run `npm init -y` to create package.json. Update it:
   - name: "claude-notify"
   - version: "0.1.0"
   - description: "Audible feedback for Claude Code using retro game sounds"
   - main: "dist/index.js"
   - scripts: { "build": "tsc", "test": "vitest run" }
   - Add "files" array including "dist", "assets" (so sounds are bundled in package)

2. Install dependencies:
   - `npm install play-sound` (runtime dep)
   - `npm install -D typescript vitest @types/node` (dev deps)

3. Create tsconfig.json:
   - target: ES2020, module: Node16, moduleResolution: Node16
   - outDir: "dist", rootDir: "src"
   - strict: true, esModuleInterop: true
   - include: ["src"]

4. Create directory structure:
   - src/audio/
   - assets/sounds/

5. Create a placeholder WAV file at assets/sounds/placeholder.wav. Generate a minimal valid WAV file (44-byte header + short silence) using a Node one-liner:
   ```
   node -e "const b=Buffer.alloc(44+8000);b.write('RIFF',0);b.writeUInt32LE(36+8000,4);b.write('WAVE',8);b.write('fmt ',12);b.writeUInt32LE(16,16);b.writeUInt16LE(1,20);b.writeUInt16LE(1,22);b.writeUInt32LE(8000,24);b.writeUInt32LE(8000,28);b.writeUInt16LE(1,32);b.writeUInt16LE(8,34);b.write('data',36);b.writeUInt32LE(8000,40);require('fs').writeFileSync('assets/sounds/placeholder.wav',b);"
   ```

6. Create src/audio/index.ts as barrel export: `export { playSound, resolveAssetPath } from './player';`

7. Create .gitignore with: node_modules/, dist/, *.js.map
  </action>
  <verify>
    <automated>cd "C:/Users/evano/repos/claude-notificaitons" && node -e "const p=require('./package.json'); console.assert(p.name==='claude-notify'); console.assert(p.dependencies['play-sound']); console.log('OK');" && test -f assets/sounds/placeholder.wav && echo "WAV exists"</automated>
  </verify>
  <done>package.json exists with play-sound dependency, tsconfig.json configured, placeholder.wav exists in assets/sounds/, directory structure created, .gitignore in place</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build and test cross-platform audio player module</name>
  <files>src/audio/player.ts, src/audio/player.test.ts</files>
  <behavior>
    - resolveAssetPath("placeholder.wav") returns absolute path to assets/sounds/placeholder.wav and the file exists at that path
    - resolveAssetPath("nonexistent.wav") throws an error indicating file not found
    - playSound("placeholder.wav") calls play-sound's play method with the resolved path (mock test -- do not actually play audio in CI)
    - playSound returns a Promise that resolves when playback completes
    - playSound rejects with descriptive error if file does not exist
  </behavior>
  <action>
1. Create src/audio/player.ts:
   - `resolveAssetPath(filename: string): string` -- resolves filename against the package's assets/sounds/ directory using path.resolve(__dirname, '../../assets/sounds/', filename). Throws if file does not exist (fs.existsSync check).
   - `playSound(filename: string): Promise<void>` -- calls resolveAssetPath, then uses play-sound to play the file. Wraps the callback-based play-sound API in a Promise. On success resolves, on error rejects with a descriptive message including the filename and platform.
   - Export both functions.
   - Note: play-sound uses `const player = require('play-sound')({})` then `player.play(filepath, callback)`.

2. Create src/audio/player.test.ts using vitest:
   - Test resolveAssetPath returns valid absolute path for placeholder.wav
   - Test resolveAssetPath throws for nonexistent file
   - Test playSound with mocked play-sound (vi.mock) -- verify it calls play with correct path
   - Test playSound rejects when file missing

Follow TDD: write tests first, run them (expect failure), then implement player.ts, run again (expect pass).
  </action>
  <verify>
    <automated>cd "C:/Users/evano/repos/claude-notificaitons" && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All vitest tests pass. resolveAssetPath correctly locates bundled assets. playSound wraps play-sound in a Promise and handles errors. Both functions exported from src/audio/index.ts.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes all audio tests
- `node -e "const {resolveAssetPath} = require('./dist/audio/player'); console.log(resolveAssetPath('placeholder.wav'))"` after `npm run build` prints absolute path to the WAV file
- placeholder.wav exists and is a valid WAV file (>44 bytes)
</verification>

<success_criteria>
- Project initialized with TypeScript, vitest, play-sound
- Audio player module resolves bundled asset paths correctly
- Audio player wraps play-sound in async/Promise API
- All tests pass
- Placeholder sound file bundled in assets/sounds/
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-and-config-foundation/01-01-SUMMARY.md`
</output>
